Archived company/contact automation lives here.

These files were moved out of the active `pages/` and `tests/` trees so they are not picked up by the default Playwright flows or npm scripts.
The JavaScript files were also renamed to `.archived.js` so they are harder to execute or import by accident.

If you rebuild company/contact automation, create new files in the active app structure instead of reusing this archive by accident.
